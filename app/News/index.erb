<div class="pageTitle">
  <h1>News</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="">Refresh</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @news.each do |news| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => news.object %>">
            <span class="title"><%= news.title %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>