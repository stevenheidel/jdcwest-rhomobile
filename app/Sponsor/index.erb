<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="">Back</a>
  </div>
  
  <h1 id="pageTitle">Sponsors</h1>
</div>

<div class="content">
  <ul>
    <% @sponsors.each do |sponsor| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => sponsor.object %>">
            <span class="title"><%= sponsor.name %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>