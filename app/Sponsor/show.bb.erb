<div class="toolbar">
  <div class="leftItem backButton">
    <a href="<%= url_for :action => :index %>">Back</a>
  </div>
  
  <h1 id="pageTitle"><%= @sponsor[:name].gsub("_", " ") %></h1>
</div>

<div id="content">

    <table>
      
      <tr>
    	  <td class="recordLabel"><img src="/app/Sponsor/pictures/<%= @sponsor[:name] %>.jpg" width="300" /></td>        
      
    	  <td class="recordLabel"><%= @sponsor[:type] %></td>
        
        <td class="recordLabel"><%= @sponsor[:description] %></td>
      </tr>
      
    </table>
</div>